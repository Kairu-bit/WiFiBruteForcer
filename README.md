# WiFi BruteForcer

## Installation
```
pkg update -y &&
pkg upgrade -y &&
pkg install git -y &&
git clone https://github.com/Kairu-bit/WiFiBruteForcer.git &&
cd WiFiBruteForcer &&
chmod +x setup.sh &&
./setup.sh
```
